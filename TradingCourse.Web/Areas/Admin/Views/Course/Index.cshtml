@model IEnumerable<TradingCourse.Application.Models.Course>
@{
    ViewData["Title"] = "Manage Courses";
}

<div class="pb-4">
    <div class="d-flex justify-content-between align-items-end mb-4">
        <div>
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="text-secondary text-decoration-none small">
                <i class="bi bi-chevron-left"></i> Dashboard
            </a>
            <h1 class="text-white fw-bold mb-0 mt-1">Manage Courses</h1>
        </div>
        <a asp-action="Create" class="btn btn-premium mb-1" id="btn-create-course">
            <i class="bi bi-plus-circle me-1"></i> Add Course
        </a>
    </div>

    <div class="glass-card" id="admin-courses-card">
        @if (Model == null || !Model.Any())
        {
            <div class="text-center py-5">
                <i class="bi bi-journal-plus fs-1 text-muted mb-3 d-block"></i>
                <h3 class="text-white">No courses created yet</h3>
                <p class="text-secondary mb-4">Click "Add Course" above to create your first course offering.</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-dark table-hover mb-0 align-middle" style="border-color: var(--card-border);">
                    <thead>
                        <tr class="text-secondary">
                            <th>ID</th>
                            <th>Course Title</th>
                            <th>Instructor</th>
                            <th>Schedule</th>
                            <th>Price</th>
                            <th>Sale Price</th>
                            <th>Type</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var course in Model)
                        {
                            <tr>
                                <td class="font-monospace text-muted">#@course.Id</td>
                                <td>
                                    <div class="d-flex align-items-center gap-3">
                                        @if (course.MediaType == "Image" && !string.IsNullOrEmpty(course.MediaUrl))
                                        {
                                            <img src="@course.MediaUrl" alt="@course.Title" class="rounded object-fit-cover shadow-sm border border-secondary border-opacity-25" style="width: 48px; height: 48px;" />
                                        }
                                        else if (course.MediaType == "Video" && !string.IsNullOrEmpty(course.MediaUrl))
                                        {
                                            <div class="bg-dark rounded d-flex align-items-center justify-content-center shadow-sm border border-secondary border-opacity-25" style="width: 48px; height: 48px;">
                                                <i class="bi bi-play-circle text-secondary fs-5"></i>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="bg-dark rounded d-flex align-items-center justify-content-center shadow-sm border border-secondary border-opacity-25" style="width: 48px; height: 48px;">
                                                <i class="bi bi-image text-secondary fs-5"></i>
                                            </div>
                                        }
                                        <div>
                                            <strong class="text-white">@course.Title</strong>
                                            <span class="d-block text-secondary small">slug: @course.Slug</span>
                                        </div>
                                    </div>
                                </td>
                                <td>@course.Instructor</td>
                                <td>@course.Schedule</td>
                                <td>₹@course.Price.ToString("N2")</td>
                                <td>@(course.SalePrice.HasValue ? "₹" + course.SalePrice.Value.ToString("N2") : "None")</td>
                                <td>
                                    @if (course.IsLive)
                                    {
                                        <span class="live-indicator"><span class="live-dot"></span> LIVE</span>
                                    }
                                    else
                                    {
                                        <span class="badge-custom">Recorded</span>
                                    }
                                </td>
                                <td class="text-end">
                                    <div class="d-inline-flex gap-2">
                                        <a asp-action="Edit" asp-route-id="@course.Id" class="btn btn-sm btn-outline-custom px-2" id="btn-edit-@course.Id" title="Edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form asp-action="Delete" asp-route-id="@course.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this course? This cannot be undone.');" style="display:inline;">
                                            <button type="submit" class="btn btn-sm btn-danger border-0 bg-opacity-25 text-danger px-2" id="btn-delete-@course.Id" style="background-color: rgba(220, 53, 69, 0.15);" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
